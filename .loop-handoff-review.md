# Dev → Review — Phase 1 contact channels (#92)

## Status

Phase 1 end-to-end implementation is complete on `issue-92` (`412117b`). PR opened: https://github.com/therealtimex/signals/pull/98

`npm run check` green (473 tests, migrations, build).

## What shipped

1. **Schema** — `contact_channels` (0017); 0018 backfills email/phone then drops legacy scalars (`platform`, `platform_user_id`, `email`, `phone`, `verified_email`, `idx_contacts_email`).
2. **Query layer** — channel types registry, CRUD, `findContactByChannel`, primary demotion invariant, `ContactDTO` with shim `email`/`phone`.
3. **Writers** — `contacts.ts` create/update route channel inputs; enrichment scores from channels.
4. **API & agents** — REST + agent tools accept `channels[]`; legacy email/phone shim preserved; platform removed from contact row (derived from identities).
5. **Importers** — Gmail contacts + LinkedIn CSV dedup via `findContactByChannel`; Gmail metadata sync reads email channels.
6. **UI** — contact form/detail use DTO email/phone; platform chip from primary identity.

## Review focus

- Migration 0018 backfill SQL correctness (email normalization, idempotent INSERT)
- Destructive column drop safety on upgrade path
- `findContactByChannel` dedup semantics vs spec ADR-092-6
- Shim compatibility for API consumers still sending `email`/`phone` without `channels[]`

## Next

Review PR #98; route to QA after approval.
