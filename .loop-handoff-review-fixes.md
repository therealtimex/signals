# Dev → Review — PR #98 review fixes

## Status

Addressed all five review blockers on `issue-92`. `npm run check` green (486 tests).

## Fixes

1. **Deferred P1e drops (Critical)** — Removed migration `0018` column drops; restored legacy scalar columns in schema. Shim release only; drops ship in a follow-up PR after G3 window. Added `ensureContactScalarColumns()` for DBs that already applied the destructive migration locally.

2. **Backfill normalization (High)** — Removed SQL backfill; wired idempotent `backfillChannels()` via `normalizeChannelValue` in instrumentation (after migrations, before identity migration). Added parity/idempotency tests.

3. **Channel write contracts (High)** — `applyLegacyEmailPhone` preserves `isVerified` when omitted; clears primary channels on empty legacy email/phone; `syncChannelInputs` for full channel replacement. Enrichment recalc runs on channel CRUD.

4. **`findContactByChannel` archived filter (High)** — Archived contacts filtered in SQL join before ordering; added collision test.

5. **Multi-channel UI + API tests (High)** — `ContactChannelsInput` component wired into contact form, detail, and add dialog. REST round-trip + legacy shim tests added.

## Also

- Broke `contacts` ↔ `contact-channels` circular import via `contact-read-model.ts`
- Fixed Vitest env init order (`setup-env.ts` before db client import)
- `ContactDTO` omits deprecated scalar fields (`platform`, etc.) while keeping shim `email`/`phone`

## Next

Re-review PR #98.
